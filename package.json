{
  "name": "voip-accelerator",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "version": "1.0.0",
  "description": "A Vue 3 project that accepts 2 CSV files and compares the two lookiing for differences in rates.",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
