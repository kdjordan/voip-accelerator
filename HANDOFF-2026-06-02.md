# Conductor Handoff — 2026-06-02

> Transient handoff so a fresh chat can resume the CONDUCTOR role after a restart.
> Durable state lives in `ORCHESTRATION.md` (live board) + auto-memory (`MEMORY.md`). This doc points
> at those — it does not duplicate them. (Supersedes HANDOFF-2026-06-01.md; nothing substantive
> changed between them — no new commits or deploys.)

---

## ▶ PASTE THIS TO START THE NEW CONDUCTOR CHAT

```
You're the CONDUCTOR for the VoIP Accelerator repo. Read these first, in order:
  1. ORCHESTRATION.md          — your role + the live "Current State" board (resume from it)
  2. .claude/CLAUDE.md         — commands, conventions, prod/deploy reality, CodeGraph
  3. Auto-memory MEMORY.md     — index; START-HERE = port-next-steps.md. Also read seo-ssg.md.
Then sanity-check: `git branch --show-current` = main, `git log --oneline -5`, origin/main in sync.

State of the world (verify against the above, don't trust this verbatim):
  - PROD = de3b691, tagged prod-2026-05-30 (SEO/SSG live). main = f67480a, origin/main in sync,
    working tree clean. Commits on main above de3b691 are board/handoff docs only (code == prod).
  - main IS the live prod branch. A push to main does NOT auto-deploy — deploy is a MANUAL Coolify
    action. Tag every prod deploy. NEVER push or deploy without owner OK.

First task this session: UPDATE CMUX (still pending from 2026-06-01 — likely the reason for this
restart). Confirm with owner what it involves (version/target; whether ORCHESTRATION.md's cmux
description needs revising) before doing anything. Don't assume.
```

---

## Where things stand (pointers, not a re-dump)

- **Live board:** `ORCHESTRATION.md` → "Current State" (top entry = SEO/SSG deploy 2026-05-30).
- **Prod baseline:** `de3b691` / tag `prod-2026-05-30`. `main` = `f67480a` (board + handoff commits on top of prod; code-identical to prod). `origin/main` in sync, working tree clean.
- **Most recent shipped + deployed + verified-live work:**
  - Unified upload progress indicators + `reportWriteProgress` helper + Studio freeze-at-90 fix — auto-memory `xlsx-upload-export.md` (follow-up b); commits `85fd043`/`a93b1fd`/`00bd202`/`6909891`.
  - SEO/SSG marketing surface — auto-memory **`seo-ssg.md`** + commit `de3b691`. ⚠️ **Build pipeline changed:** `npm run build` is now `vite-ssg build && node scripts/create-spa-fallback.mjs`. Read `seo-ssg.md` before touching the build, routing, marketing pages, or the apex→www redirect.

## Open items (none blocking; owner's call)

1. **UPDATE CMUX** — the stated focus for this session (pending since 2026-06-01). Clarify scope with owner at start — don't assume what "update cmux" means.
2. **PDF rebrand (xlsx follow-up c)** — `utils/pricing-audit-pdf.ts` + `utils/rate-gen-summary-pdf.ts` still emerald → Switchboard. See `xlsx-upload-export.md`.
3. **Search Console** — submit `https://www.voipaccelerator.com/sitemap.xml` (owner-side dashboard action) so the new marketing pages get crawled.
4. **PricingView copy** — "team licensing / multi-seat" framing on a free-forever product; owner to confirm intent (flagged, not changed). See `seo-ssg.md`.

## Access / commands (by reference — secrets NOT in this doc)

- Coolify API token, app uuid, deploy/restart/env endpoints; SSH (`telcoos-prod`); prod DB; deploy-log query → all in auto-memory `port-next-steps.md` (KEY ACCESS / COMMANDS) + `coolify-state.md` / `ssh-hetzner-host.md`.
- Dev/test/build commands + CodeGraph usage: `.claude/CLAUDE.md`. Gate before merge to main: `npm run regression-check` (from `client/`).
- chrome-devtools MCP available for visual verification at `localhost:5173` (`chrome-devtools-mcp.md`).

## Rollback (if SEO/SSG needs reverting)

Pure code, no migrations/external state: `git revert de3b691` → push → manual redeploy returns prod to `prod-2026-05-29` (`7189ff0`).

## Suggested skills for the next conductor

- **`verify`** / **`run`** — drive the running app to confirm a change works (esp. after the cmux update, re-confirm the dev/agent flow on `localhost:5173`).
- **`code-review`** / **`simplify`** — review the diff before merging any worker/own changes to main.
- **`diagnose`** — disciplined loop if the cmux update or anything else surfaces a bug.
- **`handoff`** — regenerate this doc at the next stopping point.

(Multi-agent orchestration itself is manual per `ORCHESTRATION.md` — conductor + workers in worktrees — not a skill.)
